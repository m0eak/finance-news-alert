# data/raw/

这个目录存放原始抓取结果。

## 目录用途

这里的文件应该尽量接近新闻源原始内容，只做最少量包装。

## 这里的文件通常是什么

例如：

- RSS 抓取结果 JSON
- 财联社电报抓取结果 JSON
- 合并后的原始新闻列表

## 为什么要保留原始数据

- 方便判断是“抓取没抓到”还是“下游处理出错”
- 方便后面重新跑标准化 / 聚类，而不必每次都重新请求上游
- 便于对比新闻源结构是否发生变化

## 建议格式

- 优先使用 JSON
- 保留来源字段、原始标题、原始摘要、原始时间、原始链接
